import React from "react";
import '../styles/header.css';

const Header = () => {
    return (
        <div className='header'>
            <h2>CV Generator</h2>
        </div>
    );
};

export default Header;