import react from "react";

const Summary = (props) => {
    const { editMode } = props;
    if (editMode) {
        return (
            <div>Summary</div>
        );
    } else {
        return (
            <div>Summary</div>
        );
    }
}

export default Summary;